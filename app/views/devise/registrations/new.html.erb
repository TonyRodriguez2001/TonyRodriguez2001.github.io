<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Sign up</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <style>
      body {
        background-color: #1a1a1a;
        color: white;
      }
      form {
        margin-top: 50px;
      }
      h2 {
        font-size: 2rem;
        font-weight: bold;
        margin-bottom: 30px;
      }
      label {
        margin-top: 10px;
        display: block;
        font-weight: bold;
      }
      .field {
        margin-bottom: 20px;
      }
      input[type="email"],
      input[type="password"] {
        background-color: #303030;
        border: none;
        border-radius: 0;
        color: white;
        font-size: 1.2rem;
        padding: 15px;
        width: 100%;
      }
      input[type="submit"] {
        background-color: #e50914;
        border: none;
        border-radius: 0;
        color: white;
        font-size: 1.2rem;
        padding: 15px;
        width: 100%;
      }
      .actions {
        margin-top: 20px;
      }
      a {
        color: #e50914;
      }
    </style>
  </head>
  <body>
  <div class="col-12 p-3">
        <%= image_tag('SelfDestructLogo.png', alt: 'Self Destruct Logo', height: "45", width: "400", class: "float-start me-2") %>
  </div>
  <br>
        <br>
        <br>
    <div class="container">
      <div class="row">
        <div class="col-12">
          <h2>Sign up</h2>
          <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>

            <div class="field">
              <%= f.label :email %><br />
              <%= f.email_field :email, autofocus: true, placeholder: "John.Doe@Example.com", autocomplete: "email" %>
            </div>

            <div class="field">
              <%= f.label :password %>
              <% if @minimum_password_length %>
              <em>(<%= @minimum_password_length %> characters minimum)</em>
              <% end %><br />
              <%= f.password_field :password, placeholder: "!l0v33ncrypt!0n!", autocomplete: "new-password" %>
            </div>

            <div class="field">
              <%= f.label :password_confirmation %><br />
              <%= f.password_field :password_confirmation, placeholder: "!l0v33ncrypt!0n!", autocomplete: "new-password" %>
            </div>

            <div class="actions">
              <%= f.submit "Sign up" %>
            </div>
          <% end %>

          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </body>
</html>
